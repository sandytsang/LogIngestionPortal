{
  "category": "Memory",
  "fields": [
    {
      "id": "PhysicalMemoryTotalBytes",
      "label": "Physical memory total (bytes)",
      "order": 82,
      "default": false,
      "setups": ["cs"],
      "expression": "$cs.TotalPhysicalMemory",
      "column": { "name": "PhysicalMemoryTotalBytes", "type": "long", "description": "Total installed physical memory in bytes (Win32_ComputerSystem.TotalPhysicalMemory)." }
    },
    {
      "id": "VirtualMemoryTotalBytes",
      "label": "Virtual memory total (bytes)",
      "order": 83,
      "default": false,
      "setups": ["os"],
      "expression": "if ($os.TotalVirtualMemorySize) { [int64]$os.TotalVirtualMemorySize * 1KB } else { $null }",
      "column": { "name": "VirtualMemoryTotalBytes", "type": "long", "description": "Total virtual memory in bytes (Win32_OperatingSystem.TotalVirtualMemorySize, KB -> bytes)." }
    }
  ]
}
